{
  "name": "todoist-electron",
  "version": "0.1.0",
  "description": "Todoist electron implementation ",
  "main": "main.js",
  "dependencies": {
    "electron-settings": "^3.2.0"
  },
  "devDependencies": {
    "electron": "^2.0.5",
    "electron-icon-maker": "0.0.4",
    "electron-packager": "^12.1.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/electron .",
    "make-icons": "./node_modules/.bin/electron-icon-maker --input=./assets/icons/logo_transparent.png --output=./assets",
    "build-win": "./node_modules/.bin/electron-packager . --overwrite --asar=true --platform=win32 --arch=ia32 --icon=assets/icons/win/icon.ico --prune=true --out=_builds --version-string.CompanyName=GrandSilence --version-string.FileDescription=\"Todoist based on Electron framework\" --version-string.ProductName=\"Todoist Electron\"",
    "build-linux": "./node_modules/.bin/electron-packager . --overwrite --asar=true --platform=linux --arch=x64 --icon=assets/icons/png/1024x1024.png --prune=true --out=_builds",
    "build-mac": "./node_modules/.bin/electron-packager . --overwrite --platform=darwin --arch=x64 --icon=assets/icons/mac/icon.icns --prune=true --out=_builds"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/grandsilence/todoist-electron.git"
  },
  "keywords": [
    "todoist",
    "electron",
    "tasks",
    "taskmanager"
  ],
  "author": "Grand Silence",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/grandsilence/todoist-electron/issues"
  },
  "homepage": "https://github.com/grandsilence/todoist-electron"
}
